# 星尘项目自定义配置说明

本文档记录了将星尘项目（Stardust）自定义为科控物联版本时需要修改的配置信息。

## 1. StarAgent 项目信息修改

**文件路径：** `StarAgent/StarAgent.csproj`

**修改内容：**

```xml
<AssemblyTitle>进程守护</AssemblyTitle>
<Description>持续监控你的应用，如果应用崩溃、卡顿或停止，会自动重启。它非常适合让非服务应用在后台运行，并确保它们在系统启动时自动启动，甚至在登录前就启动，无需重写为服务。QQ:2492123056</Description>
<Company>科控物联</Company>
<Copyright>©2002-2026 TC</Copyright>
```

**原版内容：**

```xml
<AssemblyTitle>星尘代理</AssemblyTitle>
<Description>星尘节点守护代理。部署于每台应用服务器或边缘节点，提供进程守护、远程发布部署、性能监控采集（CPU/内存/磁盘/网络/GPU）、日志与指标上报，支持 Windows 服务、Linux systemd 与 IoT 边缘环境。</Description>
<Company>新生命开发团队</Company>
<Copyright>©2002-2026 NewLife</Copyright>
```

## 2. 星尘服务器地址

**默认地址：** `http://47.113.219.65:6600`

**配置位置：** 需要在 StarAgent 的配置文件中修改连接的星尘服务器地址。

## 3. ILRepack 单文件打包配置

**文件路径：** 
- `StarAgent/StarAgent.csproj`
- `StarAgent/ILRepack.targets`

**配置说明：**

### StarAgent.csproj 中添加：

```xml
<ItemGroup>
  <PackageReference Include="ILRepack" Version="2.0.44" Condition="'$(TargetFramework)'=='net461'">
    <PrivateAssets>all</PrivateAssets>
  </PackageReference>
</ItemGroup>

<Import Project="ILRepack.targets" Condition="'$(TargetFramework)'=='net461'" />
```

### ILRepack.targets 文件内容：

```xml
<?xml version="1.0" encoding="utf-8" ?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="ILRepack" AfterTargets="Build" Condition="'$(TargetFramework)'=='net461'">
    <PropertyGroup>
      <OutputPathFull>$(TargetDir)</OutputPathFull>
      <ILRepackPath>$(NuGetPackageRoot)ilrepack\2.0.44\tools\ILRepack.exe</ILRepackPath>
    </PropertyGroup>
    <ItemGroup>
      <InputAssemblies Include="$(OutputPathFull)StarAgent.exe" />
      <InputAssemblies Include="$(OutputPathFull)NewLife.Agent.dll" />
      <InputAssemblies Include="$(OutputPathFull)NewLife.Core.dll" />
      <InputAssemblies Include="$(OutputPathFull)NewLife.Remoting.dll" />
      <InputAssemblies Include="$(OutputPathFull)Stardust.dll" />
      <InputAssemblies Include="$(OutputPathFull)System.Buffers.dll" />
      <InputAssemblies Include="$(OutputPathFull)System.Memory.dll" />
      <InputAssemblies Include="$(OutputPathFull)System.Numerics.Vectors.dll" />
      <InputAssemblies Include="$(OutputPathFull)System.Runtime.CompilerServices.Unsafe.dll" />
      <InputAssemblies Include="$(OutputPathFull)System.ValueTuple.dll" />
      <InputPdbs Include="$(OutputPathFull)StarAgent.pdb" />
      <InputPdbs Include="$(OutputPathFull)NewLife.Agent.pdb" />
      <InputPdbs Include="$(OutputPathFull)NewLife.Core.pdb" />
      <InputPdbs Include="$(OutputPathFull)NewLife.Remoting.pdb" />
      <InputPdbs Include="$(OutputPathFull)Stardust.pdb" />
      <InputPdbs Include="$(OutputPathFull)System.Buffers.pdb" />
      <InputPdbs Include="$(OutputPathFull)System.Memory.pdb" />
      <InputPdbs Include="$(OutputPathFull)System.Numerics.Vectors.pdb" />
      <InputPdbs Include="$(OutputPathFull)System.Runtime.CompilerServices.Unsafe.pdb" />
      <InputPdbs Include="$(OutputPathFull)System.ValueTuple.pdb" />
    </ItemGroup>
    <Exec Command="&quot;$(ILRepackPath)&quot; /out:&quot;$(OutputPathFull)StarAgent.merged.exe&quot; /target:exe /targetplatform:v4 /internalize /ndebug @(InputAssemblies->'&quot;%(FullPath)&quot;', ' ')" />
    <Delete Files="@(InputAssemblies)" />
    <Delete Files="@(InputPdbs)" />
    <Delete Files="$(OutputPathFull)StarAgent.exe.config" />
    <Move SourceFiles="$(OutputPathFull)StarAgent.merged.exe" DestinationFiles="$(OutputPathFull)StarAgent.exe" />
  </Target>
</Project>
```

## 4. 部署策略修复（重要）

**文件路径：** `StarAgent/Managers/DeployStrategyBase.cs`

**问题描述：**
原版代码在查找可执行文件时，使用的是服务名称（`context.Name`）而不是配置的 `FileName`。这导致配置 `FileName=node.exe` 时，系统会错误地把参数中的 `main.js` 当作可执行文件，从而报错 193。

**修改位置：** `RetrieveExeFile` 方法

**修改后的代码：**

```csharp
protected Boolean RetrieveExeFile(DeployContext context, String workDir)
{
    var args = context.Arguments;
    var service = context.Service;

    // 优先使用配置的 FileName 作为可执行文件名
    var fileName = service?.FileName;
    FileInfo? runfile = null;

    if (!fileName.IsNullOrEmpty())
    {
        // FileName 可能是完整路径
        if (File.Exists(fileName))
        {
            runfile = new FileInfo(fileName);
        }
        // 或者是相对于工作目录的路径
        else
        {
            var fullPath = Path.Combine(workDir, fileName);
            if (File.Exists(fullPath))
            {
                runfile = new FileInfo(fullPath);
            }
        }

        // 如果 FileName 是系统命令（如 node.exe），直接使用它
        if (runfile == null && !fileName.Contains('/') && !fileName.Contains('\\'))
        {
            // 可能是 PATH 中的命令，直接使用
            context.WriteLog("使用系统命令：{0}", fileName);
            context.ExecuteFile = fileName;
            // Arguments 保持不变
            return true;
        }
    }

    // 如果没有配置 FileName 或找不到，则在工作目录中查找
    if (runfile == null)
    {
        runfile = FindExeFile(workDir, context.Name, ref args);
    }
    if (runfile == null)
    {
        context.WriteLog("无法找到可执行文件");
        return false;
    }

    context.ExecuteFile = runfile.FullName;
    context.Arguments = args;

    return true;
}
```

**原版代码：**

```csharp
protected Boolean RetrieveExeFile(DeployContext context, String workDir)
{
    var args = context.Arguments;
    var runfile = FindExeFile(workDir, context.Name, ref args);
    if (runfile == null)
    {
        context.WriteLog("无法找到可执行文件");
        return false;
    }

    context.ExecuteFile = runfile.FullName;
    context.Arguments = args;

    return true;
}
```

**修复效果：**
- 支持 `FileName=node.exe` 配置，正确启动 Node.js 应用
- 支持 `FileName=java` 配置，正确启动 Java 应用
- 支持任何系统 PATH 中的命令作为启动文件
- 保持向后兼容，原有的 zip 包部署方式不受影响

## 5. 控制台隐藏服务器地址

**文件路径：** `StarAgent/Program.cs`

**修改内容：**
注释掉显示服务器地址的代码，避免泄露服务器信息。

```csharp
// 出于安全考虑，不再控制台显示服务端地址
// if (!set.Server.IsNullOrEmpty()) Console.WriteLine("服务端：{0}", set.Server);
```

## 6. 更新后操作流程

每次从 GitHub 拉取新代码后，需要执行以下操作：

1. **检查项目文件变更**
   - 查看 `StarAgent/StarAgent.csproj` 是否被重置
   - 重新修改 AssemblyTitle、Description、Company、Copyright 等信息

2. **检查 ILRepack 配置**
   - 确认 `StarAgent/StarAgent.csproj` 中的 ILRepack 包引用是否存在
   - 确认 `Import Project="ILRepack.targets"` 语句是否存在
   - 确认 `StarAgent/ILRepack.targets` 文件是否存在

3. **检查部署策略修复**
   - 确认 `StarAgent/Managers/DeployStrategyBase.cs` 中的 `RetrieveExeFile` 方法是否需要重新修改
   - 这是启动 Node.js 等系统命令的关键修复

4. **检查控制台输出**
   - 确认 `StarAgent/Program.cs` 中服务器地址输出是否被注释

5. **检查配置文件**
   - 检查 StarAgent 的配置文件中的星尘服务器地址
   - 确保连接到正确的服务器：`http://47.113.219.65:6600`

6. **重新构建**
   - 执行 `dotnet restore`
   - 执行 `dotnet build -f net461 -c Release`
   - 验证输出目录中是否只有单个 EXE 文件

## 7. 版本信息

- **当前星尘版本：** v3.7.2026.0403
- **ILRepack 版本：** 2.0.44
- **NewLife.Agent 版本：** 10.15.2026.402

## 8. 联系方式

- **QQ：** 2492123056
- **公司：** 科控物联
